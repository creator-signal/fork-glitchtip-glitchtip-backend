# Django admin doesn't support composite primary keys
# Use SQL or the API to view logs:
#
#   SELECT id, timestamp, level, body, data
#   FROM logs_logevent ORDER BY id DESC LIMIT 20;
#
# Or via API:
#   GET /api/0/organizations/{slug}/logs/
